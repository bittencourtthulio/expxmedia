---
description: Cria o .env da instalação, mostra o que já funciona sem chave e orienta, capacidade por capacidade, onde conseguir cada chave (colada no arquivo, nunca na conversa).
argument-hint: "[capacidade, ex.: narrar]"
---

Siga a skill `ambiente` do núcleo do ExpxMedia (`skills/ambiente/SKILL.md`).

Se `$ARGUMENTS` nomear uma capacidade (por exemplo `narrar` ou `publicar`), depois de garantir que o
`.env` existe vá direto ao passo "Ligar uma capacidade" para ela.
